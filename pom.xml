<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.autocare</groupId>
    <artifactId>autocare-garage</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>AutoCare Garage</name>
    <description>Vehicle servicing platform - three Spring Boot microservices</description>

    <modules>
        <module>customers-service</module>
        <module>workshop-service</module>
        <module>web-ui</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <!-- Override Spring Boot's managed version: the one it pins still carries
             the pgjdbc SCRAM advisories that Trivy flags. -->
        <postgresql.version>42.7.13</postgresql.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jacoco.version>0.8.12</jacoco.version>
        <sonar-maven-plugin.version>5.7.0.6970</sonar-maven-plugin.version>
        <sonar.projectKey>autocare-garage</sonar.projectKey>
        <sonar.projectName>AutoCare Garage</sonar.projectName>
        <sonar.coverage.jacoco.xmlReportPaths>${project.build.directory}/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals><goal>prepare-agent</goal></goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Declared here so "mvn sonar:sonar" resolves the prefix and the
                 scanner version is pinned rather than floating to latest. -->
            <plugin>
                <groupId>org.sonarsource.scanner.maven</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
                <version>${sonar-maven-plugin.version}</version>
            </plugin>
        </plugins>
    </build>
</project>

server:
  port: 8081

spring:
  application:
    name: customers-service
  # Datasource is entirely environment driven. With no variables set it falls
  # back to in-memory H2, so local development and tests need no database.
  # In Kubernetes the Deployment supplies PostgreSQL values from a Secret.
  datasource:
    url: ${DB_URL:jdbc:h2:mem:customersdb;DB_CLOSE_DELAY=-1}
    driver-class-name: ${DB_DRIVER:org.h2.Driver}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}
    hikari:
      maximum-pool-size: ${DB_POOL_SIZE:5}
      connection-timeout: 10000
  jpa:
    hibernate:
      # create-drop for throwaway H2, update for a real database that must
      # survive a pod restart.
      ddl-auto: ${DDL_AUTO:create-drop}
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-console

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
  info:
    env:
      enabled: true

info:
  app:
    name: AutoCare Customers Service
    description: Owns customers and their vehicles

logging:
  level:
    com.autocare: INFO
